extra:
  packages:
    - ntfs-3g
  mounts:
    ssd:
      src: /dev/sda2
      type: ext4
  paths:
    gitlab-pv: /mnt/ssd/k8s-local-pv/gitlab
  metal:
    # NOTE: Reserved these in the Netgear router by setting the upper
    #       limit of the router's IP range to a.b.c.199
    pool: 192.168.1.200-192.168.1.215
