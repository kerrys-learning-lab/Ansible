# code: language=ansible
cloudgpu_k8s_applications:
  gpu:
    enabled: true

  cnpg:
    enabled: true
    local_volumes:
      cnpg--cloud-lab-inference--open-webui-db:
        capacity: 10Gi
        path: cnpg/cloud-lab/inference/open-webui-db
        mode: u=rwx,go=
