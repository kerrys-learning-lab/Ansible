configs:
{% for key, value in rke2_config['registries'].items() %}
  {{ key }}:
    auth:
      username: {{ value.username }}
{% if 'password' in value %}
      password: {{ value.password }}
{% else %}
      token: {{ value.token }}
{% endif %}
{% endfor %}
