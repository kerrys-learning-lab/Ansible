podman_defaults:
  packages:
    - podman
    - podman-compose
    - podman-docker
    - podman-remote
    - podman-compose
  service:
    name: podman.service
    unit_path: /usr/lib/systemd/system/podman.service
  users: []
  group: podman
  registries: {}
